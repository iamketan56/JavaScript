/*function greet(nickname)
{
    console.log(`Hello ${nickname}`)
}
greet('ketan')
//example 2
function rollDie()
{
    let roll = Math.floor(Math.random() * 6)
    console.log(`Rolled: ${roll}`)
}
function throwdice(times) // function with one argument
{
    for (let i = 1; i <= times; i++)
    {
        rollDie()
        }
}
throwdice(3)
*/
function details(name,age,section)
{
    console.log(`Details of ${name} are : He is ${age} years old and from section ${section}`)
}
details('ketan',21,'IT')